name: Apply TOH Rescue

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  parked:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Rescue workflow parked while the final patch is prepared."
